[package]
name = "vibe_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "vibe_engine"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
numpy = "0.22"
serde_json = "1.0"
